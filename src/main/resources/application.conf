akka {
  loglevel = DEBUG
}

http {
  interface = "0.0.0.0"
  port = 9000
}

merit {
  startAmount = 11
  payoutThreshold = 0.5

  usersFile = "data/users.json"
  transactionsFile = "data/transactions.json"
}